local globals = {}
    
--------------------------------------------------------------------------------
-- Day Change Events.
--------------------------------------------------------------------------------
globals[1] = function(new, old)
    --local name   = windower.ffxi.get_player().name:lower()
    --local server = res.servers[windower.ffxi.get_info().server].en:lower()
        
    --helpers["stats"].updateData()
    --bpcore:upload("bp/export/"..server.."/"..name, ".json")
    
end

return globals